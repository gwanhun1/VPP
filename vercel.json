{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "buildCommand": "yarn install && yarn build",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
