{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "buildCommand": "yarn install && yarn nx build web",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
