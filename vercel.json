{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "installCommand": "corepack enable && corepack yarn --version && corepack yarn install --immutable",
  "buildCommand": "NX_NO_CLOUD=1 CI=1 corepack yarn nx build web --output-style=stream",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
