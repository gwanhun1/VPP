{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "buildCommand": "yarn install && yarn nx build web",
  "outputDirectory": "dist/apps/web",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
