{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "buildCommand": "yarn nx build web --with-deps --skip-nx-cache",
  "outputDirectory": "dist/apps/web",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
